export interface Student {
  studentNumber: number;   // 5 digit number
  firstName: string;       // max 30 char
  lastName: string;        // max 30 char
  classNumber: number;     // 2 digit number
}
